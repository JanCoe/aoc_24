pub fn func(data: &str) -> usize {
    todo!()
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_func() -> () {
        let data: &str = "";
        assert_eq!(9, func(data));
    }
}
